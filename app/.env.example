# Required — from your Supabase project (Settings -> API).
EXPO_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Optional — leave unset to use mock providers (app runs fully offline/free).
# Each is wired up independently: set only the ones you have.
#
# Easiest path: set ONLY EXPO_PUBLIC_GEMINI_API_KEY (free, no credit card,
# from https://aistudio.google.com/apikey) — it covers all three roles below
# by itself. The other keys are purpose-built alternatives that take
# priority over Gemini for their specific role if you also set them.
EXPO_PUBLIC_GEMINI_API_KEY=
EXPO_PUBLIC_ANTHROPIC_API_KEY=
EXPO_PUBLIC_SARVAM_API_KEY=
EXPO_PUBLIC_ELEVENLABS_API_KEY=
EXPO_PUBLIC_GOOGLE_VISION_API_KEY=
