{
  "name": "@continuum/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Shared capture -> structure -> compare -> flag -> handoff engine. Used by both the community health worker voice scenario and the hospital discharge photo scenario.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsx src/__tests__/rules.ts && tsx src/__tests__/smoke.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  }
}
